# Tests

The parser suite uses synthetic, anonymized HTML fixtures. Real account pages,
credentials, cookies, card numbers, and personal loan data must never be added
to this directory. See `docs/fixtures.md` for the sanitizing checklist.
