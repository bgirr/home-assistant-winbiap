# Tests

Sanitized HTML fixtures and parser tests will be added in Phase 0. Fixtures
must never contain names, library card numbers, session cookies, barcodes tied
to a person, or other account-specific data.
